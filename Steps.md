1) Install uv
    - 'pip install uv'

2) Create a view and activate it 
    - 'uv.venv'
    -'.venv\Scripts\activate'

3) 'pip install fastapi[standard]'

4) 'uvicorn main:app --reload'

5) 'git add .'
'git commit -m "your message"'
'git push'